import { RepoFilterPipe } from './repo-filter.pipe';

describe('RepoFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RepoFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
